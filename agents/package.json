{
  "name": "@climateshield/agents",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "ClimateShield specialist AI agents (risk, cascade, dispatch, comms, validation) with grounding + deterministic fallback.",
  "main": "./dist-cjs/index.js",
  "types": "./dist-cjs/index.d.ts",
  "exports": {
    ".": "./dist-cjs/index.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:cjs": "tsc -p tsconfig.build.json && node scripts/write-cjs-marker.cjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.3"
  }
}
