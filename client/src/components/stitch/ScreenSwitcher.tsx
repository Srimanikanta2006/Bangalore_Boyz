import React from 'react';

/**
 * ScreenSwitcher was previously used during design demo phase to switch arbitrary Stitch screens.
 * Per user request, it has been cleanly removed to enforce authentic role-based routing.
 */
export const ScreenSwitcher: React.FC = () => {
  return null;
};

export default ScreenSwitcher;
