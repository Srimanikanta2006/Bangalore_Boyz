# Public frontend configuration only — never put secrets in Vite env vars.
# MOCK uses in-browser mock services; BACKEND calls the Node/Express + Prisma API.
VITE_API_MODE=MOCK
VITE_API_BASE_URL=http://localhost:3001
